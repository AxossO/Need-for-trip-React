import React from "react";

const stats = ({ arrayNumber, setArrayNumber }) => {
  return (
    <div className="stats">
      <div></div>You Have In Your list {arrayNumber.length}
    </div>
  );
};

export default stats;
